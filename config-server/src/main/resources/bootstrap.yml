spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://event-drive-microservices-practice
          clone-on-start: true
    fail-fast: true