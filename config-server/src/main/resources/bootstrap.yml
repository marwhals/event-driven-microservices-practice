spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marwhals/spring-cloud-config-server-repository.git
          username: marwhals
          password: TOKEN_HERE
          clone-on-start: true
    fail-fast: true
  security:
    user:
      name: spring_cloud_user
      password: springCLoud_Pwd!

log:
  app-name: config-server