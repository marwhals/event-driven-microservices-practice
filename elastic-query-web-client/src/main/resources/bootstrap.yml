spring:
  application:
    name: elastic_query_web
  profiles:
    active: elastic_query_web
  cloud:
    config:
      name: elastic-query-web-client,config-client
      uri: http://localhost:8888
      username: spring_cloud_user
      password: 'password'

eureka:
  client:
    serviceUrl:
      defaultZone: http:discover-service-1:8761/eureka/,http://discovery-service-2:8762/eureka/